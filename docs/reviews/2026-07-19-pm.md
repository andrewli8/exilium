# PM review — 2026-07-19

Real user today: tiny intersection (serious traders × Node-comfortable × terminal-tolerant); the one uncontested segment is AI-agent trading research via MCP. Funnel leaks: discovery (unknown), install (git clone filters ~95%), trust (POESESSID), league-end retention cliff. Weekly-use: live, watches+Discord, dashboard. Resume features for median trader: journal, backtest, arb table, TUI (journal/backtest still valuable internally for tuning).

Roadmap calls: un-defer GGG OAuth (gates stash net-worth, the retention loop) [deferred by owner decision]; single-binary release over npm publish; missing: league-start mode (annual adoption window), dump-tab sell sheet (paste counts → priced bulk WTS message), build-cost pricing (uniques-only). Stop: deepening the signal engine until existing detectors prove out.

Ranked six: 1 OAuth application (S) [deferred], 2 binary releases + npm publish (M), 3 dump-tab sell sheet (M), 4 league-start mode before next league (M), 5 stash net-worth (M, post-OAuth), 6 Discord bot front-end for watch engine (M/L).
